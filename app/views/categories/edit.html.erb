<% content_for :title, "Editing #{@category.name}" %>
<h1 class="main-title">
	Editing Category
	<span><%= link_to 'Cancel and Return', @category, :class => 'cancel-link' %></span>
</h1>

<%= render :partial => 'form', :locals => { :category => @category, :button_text => 'Update' } %>

